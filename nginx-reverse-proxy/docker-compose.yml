version: '3'
services:
  node-be:
    build: node-backend
    environment:
     - SERVER_NAME=A
  node2-be:
    build: node-backend
    environment:
     - SERVER_NAME=B
  nginx-fe:
    build: nginx-frontend
    ports:
    - "80:80"