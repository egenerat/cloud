
check if nginx configuration can be split in 2 files
check if base url is working (requires to build a simple website):
	- static resources
	- ajax resources

- stop serving static resources from 
[error] 7#7: *1 "/etc/nginx/html/index.html" is not found (2: No such file or directory), client: 172.19.0.1, server: 127.0.0.1, request: "GET / HTTP/1.1", host: "localhost"

Use site-available and site-enabled